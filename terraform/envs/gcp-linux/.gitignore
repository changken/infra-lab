# Terraform 敏感檔案（含 GCP key，絕對不可 commit）
*.tfvars
*.tfvars.json

# GCP Service Account Key（務必加入）
*.json
!terraform.lock.hcl.json

# Terraform 狀態（含基礎設施資訊）
*.tfstate
*.tfstate.backup
tfplan

# Terraform 工作目錄
.terraform/
.terraform.lock.hcl

# 金鑰檔
*.pem
*.key
